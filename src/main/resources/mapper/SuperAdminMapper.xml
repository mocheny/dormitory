<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatic.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.it985.dormitory.mapper.SuperAdminMapper">

<!--    <select id="findByUsernameAndPassword" resultType="com.it985.dormitory.entity.SuperAdmin">
        select * from tab_superadmin where username=#{username} and password=#{password}
    </select>-->

</mapper>